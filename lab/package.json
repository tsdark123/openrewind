{
  "name": "openrewind-orion-scenario-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate:schemas": "tsx scripts/generate-schemas.ts",
    "generate:fixture": "tsx reference/generate-fixture.ts",
    "generate:smoke-responses": "tsx scripts/generate-smoke-fixtures.ts",
    "fixture:run": "tsx runner/run-fixture.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10",
    "zod": "^3.24.0",
    "zod-to-json-schema": "^3.25.0"
  }
}
