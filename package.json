{
  "name": "openrewind",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "cd frontend && pnpm dev",
    "frontend:build": "cd frontend && pnpm build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
