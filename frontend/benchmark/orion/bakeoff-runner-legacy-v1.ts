// Legacy V1 / historical-v1 contract wrapper for the bake-off runner.
// Re-exports the original runner so versioned consumers can import it explicitly.
export { runOneRepetition, runBakeoff } from './bakeoff-runner';
